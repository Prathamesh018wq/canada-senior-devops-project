apiVersion: v1
kind: Service

metadata:
  name: canada-devops-service

spec:
  selector:
    app: canada-devops-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: LoadBalancer
